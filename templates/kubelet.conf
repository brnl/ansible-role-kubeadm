KUBELET_EXTRA_ARGS="--cluster-dns={{ kubeadm_cluster_dns }} --cluster-domain={{ kubeadm_cluster_domain }} --fail-swap-on=false --cgroup-driver={{ kubeadm_cgroup_driver }} --pod-manifest-path=/etc/kubernetes/manifests --allow-privileged=true --client-ca-file=/etc/kubernetes/pki/ca.crt --cadvisor-port=0 --cgroup-driver=systemd --rotate-certificates=true --cert-dir=/var/lib/kubelet/pki --network-plugin=cni --cni-conf-dir=/etc/cni/net.d --cni-bin-dir=/opt/cni/bin"
